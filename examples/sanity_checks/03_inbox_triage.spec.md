# Pat's task — inbox triage three-way router

> *"Watch my Gmail inbox for unread messages (up to 20 at a time).
> Classify each email by urgency: urgent, normal, or low. Show me
> the urgent ones in the terminal, archive the normal ones to JSONL,
> and drop the low-priority ones."*

Expected coverage: three-way router with field-equality conditions,
each branch going to a different sink.
