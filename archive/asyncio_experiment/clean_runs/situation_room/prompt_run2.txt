I want to build a small Python program that does this:

I want a daily intelligence digest. Pull articles from three world-news
RSS feeds (BBC, NPR, Al Jazeera). For each article, classify the
severity (low / medium / high / critical), identify the geographic
location, and write a short briefing. Show me critical-severity
briefings in my terminal; save all of them to a JSONL file.

Use the Anthropic API for the classification, location-identification,
and briefing steps.

Please structure the code as follows so a tool of mine can parse it:

- Use asyncio.
- Each LLM-driven agent is a top-level `async def agent_name(message)`
  that takes a JSON-serializable dict and returns a JSON-serializable
  dict. Its docstring describes the agent's job.
- Each Python-driven stateful agent (if any) is a class with `__init__`
  for config and one method `def process(self, message)` (or
  `async def`) that takes and returns a message.
- There is exactly one function `process_one(message)` that wires the
  agents together. This is the per-item pipeline body. Use
  `asyncio.gather` for independent steps.
- Sources are declared at module top in a dict like
  `SOURCES = {"bbc": ..., "npr": ..., "al_jaz": ...}`.
- Sinks are called as `send_to(message, "sink_name", **kwargs)`.
  Provide a small `send_to` helper that does the actual print/write
  based on the sink name.

A single .py file, please.
