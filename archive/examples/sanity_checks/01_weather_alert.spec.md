# Pat's task — weather alert

> *"Check the weather every 10 minutes. Classify each report by
> severity. Show me severe-weather reports in my terminal; drop
> everything else."*

Expected coverage: simple pipeline with a binary router.
