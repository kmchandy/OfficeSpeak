# Pat's task — debate-style refinement with explicit bound

> *"I want a problem solver. An agent proposes a solution, another
> critiques it, and a judge decides if it's good enough. If not, the
> proposer tries again — but stop after three rounds either way.
> Save approved answers and gave-up cases to separate JSONL files."*

Expected coverage: feedback loop (back-edge) with explicit iteration
counter step and termination guards.
