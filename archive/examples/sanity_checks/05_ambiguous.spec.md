# Pat's task — ambiguous (should produce an ERROR)

> *"Make me a really cool app that does AI stuff."*

Expected coverage: failure-mode guard §8. Claudette should not
guess; she should emit an `# ERROR:` line.
